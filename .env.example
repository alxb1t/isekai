# --- Obsidian vault path (the PM-side project folder Claude Code reads + edits) ---
# VAULT_PROJECT_DIR = the project folder; the active plan is the highest-versioned
# implementation_plans/vX.Y_implementation_plan.md (ignore archive/).
VAULT_PROJECT_DIR="/path/to/vault/Lab/isekai"

# --- RunPod deploy config ---
RUNPOD_API_KEY=""
RUNPOD_VOLUME_ID=""
RUNPOD_GPU_TYPE="NVIDIA GeForce RTX 4090"
RUNPOD_DATACENTER=""
